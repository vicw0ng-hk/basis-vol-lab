# Phase A: skeleton only.
#
# Real resources (Cloudflare Pages, R2, D1, AWS Lambda + API Gateway, IAM,
# DNS) land in phases B–F per docs/planning/8.cloud-plan.md. This file is
# intentionally empty so that `terraform validate` succeeds against the
# bare workspace before any cloud state is created.
