[project]
name = "basis-connectors"
version = "0.1.0"
description = "Async WebSocket/REST collectors for Deribit and Binance"
requires-python = ">=3.14"
dependencies = [
    "basis-contracts",
]

[tool.uv.sources]
basis-contracts = { workspace = true }
