[project]
name = "basis-persistence"
version = "0.1.0"
description = "SQLite/D1 metadata and Parquet/R2 time-series storage"
requires-python = ">=3.14"
dependencies = [
    "basis-contracts",
    "pyarrow>=20.0",
]

[tool.uv.sources]
basis-contracts = { workspace = true }
