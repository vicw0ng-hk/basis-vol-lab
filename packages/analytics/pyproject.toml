[project]
name = "basis-analytics"
version = "0.1.0"
description = "Pandas/NumPy/SciPy analytics and signal computation"
requires-python = ">=3.14"
dependencies = [
    "basis-contracts",
    "numpy>=2.0",
    "pandas>=2.2",
    "scipy>=1.13",
    "pyarrow>=17",
    "httpx>=0.28",
]

[tool.uv.sources]
basis-contracts = { workspace = true }
